<div class="container">
<div class="row">
	<div class="col-xs-3">

		
		<%= render 'users/profile', book: @book, user: @user %>
        <%= render 'relationships/follow_button', user: @user %>
		<h2>New book</h2>
		<%= render 'books/newform', book: Book.new %>

	</div>

	<div class="col-xs-9">
		<h2>Book detail</h2>
		<% if @book_comment.errors.any? %>
          <div id="error_explanation" class="alert alert-danger">
          <%= pluralize(@book_comment.errors.count, "error")%> prohibited this odj from being saved:
            <ul>
  	           <% @book_comment.errors.full_messages.each do |message| %>
  	           <li><%= message %></li>
  	           <% end %>
            </ul>
          </div>
        <% end %>
        <div id="favorites_<%= @book.id %>">
	      <%= render partial: 'favorites/show', locals: { book: @book, user: @user} %>
		</div>
		  



			<div id="comments_area">
			  <%= render 'book_comments/index', book: @book %>
            </div>
			<% if user_signed_in? %>
			  <%= render 'book_comments/form', book: @book %>
			<% end %>
        </div>
	</div>
</div>

