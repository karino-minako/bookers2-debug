<%= form_with(model: [book, BookComment] ) do |form| %>
  <div>
  	<%= form.text_area :comment, size: "90×5" %>
  </div>
  <div class="actions">
  	<%= form.submit "コメントする" %>
  </div>
<% end %>